name: "Setup Production Env"
description: "Writes out the .env.production file from secrets"
runs:
  using: "composite"
  steps:
    - run: |
        echo "PLASMO_PUBLIC_WEATHER_API_KEYS=${{ env.PLASMO_PUBLIC_WEATHER_API_KEYS }}" >> .env.production
      shell: bash
