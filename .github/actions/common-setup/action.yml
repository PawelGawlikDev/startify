name: "Common Setup"
runs:
  using: "composite"
  steps:
    - uses: pnpm/action-setup@v5
      with:
        version: 10
    - uses: actions/setup-node@v6
      with:
        node-version: 20
        cache: "pnpm"
